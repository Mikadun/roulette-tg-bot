from random import randint, choice
from secrets import choice
from index import bot


def f_roulette(players, good = int(round(len(players)/6)), bad = int(len(players)/6), win = "#fio# win", lose = "#fio# lose"):

def chose_line(text):
    return choice(text)

def random(A, B):
    return randint(A, B)